# Contributing to HyperCanvas AI

1. Fork the repo.
2. Create a feature branch: `git checkout -b feat/custom-connectors`
3. Verify build: `npm run build`
4. Commit: `git commit -m "feat: add orthogonal connector routing"`
5. Push & open a Pull Request!
